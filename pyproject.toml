[project]
name = "iain_computer_science"
version = "0.0.1"

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
pythonpath = ["."]

[tool.ty.environment]
python = ".venv/bin/python"
